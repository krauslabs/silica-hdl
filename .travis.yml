language: rust

script:
  - rustup component add rustfmt
  - cargo fmt --all -- --check
  - cargo build --verbose
  - cargo test --verbose

rust:
  - stable

cache: cargo

branches:

  only:
    - master
